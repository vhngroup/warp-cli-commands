## Cloudflare one client Wrap Commands CLI ##
*  ### Add exclude ip tunnel ###
    * ` warp-cli tunnel ip add your_ip_format_192.168.1.1` **no add /32 or :32**
*  ### Add exclude host tunnel ###
    * ` warp-cli tunnel host add your_url `
